from collections import Counter
def freq(lst):
    temp=Counter(lst)
    result=[]
    for key,value in temp.items():
        if value>=2:
            result.append(key)
    return result        


lst=list(map(str,input().split()))
print(freq(lst))
